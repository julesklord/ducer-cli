name: 'Performance Tests: Nightly'

on:
  schedule:
    - cron: '0 3 * * *' # Runs at 3 AM every day
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: 'read'

jobs:
  perf-test:
    name: 'Run Performance Usage Tests'
    runs-on: 'gemini-cli-ubuntu-16-core'
    if: "github.repository == 'google-gemini/gemini-cli'"
    steps:
      - name: 'Checkout'
        uses: 'actions/checkout@v4' # ratchet:actions/checkout@v5

      - name: 'Set up Node.js'
        uses: 'actions/setup-node@v4' # ratchet:actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: 'Install dependencies'
        run: 'npm ci'

      - name: 'Build project'
        run: 'npm run build'

      - name: 'Run Performance Tests'
        run: 'npm run test:perf'
