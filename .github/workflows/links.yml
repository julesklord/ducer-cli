name: 'Links'

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: '00 18 * * *'

permissions:
  contents: read

jobs:
  linkChecker:
    if: |-
      ${{ github.repository == 'julesklord/ducer-cli' }}
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8' # ratchet:actions/checkout@v5

      - name: 'Link Checker'
        id: 'lychee'
        uses: 'lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411' # ratchet: lycheeverse/lychee-action@v2.8.0
        with:
          args: '--root . --verbose --no-progress --accept 200,503 ./**/*.md'
